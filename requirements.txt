# ═══════════════════════════════════════════════════════════════
# Módulo — Modelos generativos profundos · DGTIC-UNAM
#
# Aquí va SÓLO lo que Colab no trae preinstalado.
#
# No agregue torch, torchvision, numpy, pandas, scipy,
# scikit-learn ni matplotlib. Reemplazar cualquiera de esos en una
# sesión de Colab ya iniciada deja el numpy del kernel a medias
# (ImportError en numpy._core.umath, y falla toda celda que use
# scikit-learn) y sólo se arregla reiniciando el entorno de
# ejecución. Fijar torch, además, cuesta ~2.5 GB de descarga y
# varios minutos en cada cuaderno.
#
# peft es la única dependencia real del módulo (S4-E1, LoRA).
# Pide numpy>=1.17 y torch>=1.13 sin cota superior, así que se
# instala sobre lo que Colab ya tiene sin desplazar nada.
# ═══════════════════════════════════════════════════════════════
peft>=0.20
